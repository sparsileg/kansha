# Auto detect text files; normalize to LF in the repo and working tree
# (keeps patches applying cleanly on both Kubuntu and Windows)
* text=auto eol=lf

# Binary assets
*.png binary
*.ico binary
*.icns binary
