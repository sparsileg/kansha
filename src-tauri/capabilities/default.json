{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main Kansha window. No filesystem, shell, or network access beyond what is explicitly added when a feature needs it (SECU-070, R5).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-close"
  ]
}
